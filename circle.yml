machine:
  php:
    version: 7.0.4
  node:
    version: 5.7.1

test:
  pre:
    - sed -i 's/^;//' ~/.phpenv/versions/$(phpenv global)/etc/conf.d/xdebug.ini
  override:
    - phpunit --coverage-html $CIRCLE_ARTIFACTS

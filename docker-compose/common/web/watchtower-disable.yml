services:
    backend:
        labels:
            - 'com.centurylinklabs.watchtower.enable=false'

    frontend-web:
        labels:
            - 'com.centurylinklabs.watchtower.enable=false'

    rabbitmq:
        labels:
            - 'com.centurylinklabs.watchtower.enable=false'

    webhooker:
        labels:
            - 'com.centurylinklabs.watchtower.enable=false'
